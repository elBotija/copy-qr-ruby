- unless flash.empty?
  - flash.each do |name, msg|
    - if msg.present?
      - style = name == 'notice' ? 'success' : 'danger'
      %div{class: "alert alert-#{style} alert-dismissible fade show position-fixed top-0 start-50 translate-middle-x mt-3", role: 'alert'}
        = msg
        %button.btn-close{type: 'button', 'data-bs-dismiss': 'alert', 'aria-label': 'Close'}
